{
  "name": "tourism",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check src/server.mjs && node --check public/app.js && node --check test/server.test.mjs",
    "validate:content": "node scripts/validate-content.mjs",
    "start": "node src/server.mjs",
    "test": "node --test test/**/*.test.mjs"
  }
}
